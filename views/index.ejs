


<%- include('layout/header')  %>


<style>
  /* override Bootstrap table-dark header color */
  .table.table-dark thead th {
    background-color: #63E6BE !important;
    color: #ffffff !important;
    border-top: 1px solid #63E6BE !important;
    border-left: 1px solid #63E6BE !important;
    border-right: 1px solid #63E6BE !important;
    border-bottom: 1px solid #4d5154 !important;
  }
  .table.table-dark thead th:not(:last-child) {
    border-right: 1px solid white !important;
  }

  .hide{
    display: none !important;
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', function () {
    const alertEl = document.querySelector('.alert');
    if (!alertEl) return;
    const delayMs = 3000; // change to desired delay (milliseconds)

    setTimeout(function () {
      // use Bootstrap API if available
      if (typeof bootstrap !== 'undefined' && bootstrap.Alert) {
        const bsAlert = new bootstrap.Alert(alertEl);
        bsAlert.close();
      } else {
        // fallback: remove 'show' to trigger CSS fade then remove element
        alertEl.classList.remove('show');
        setTimeout(() => alertEl.remove(), 3000); // match CSS transition time
      }
    }, delayMs);
  });
</script>
<script>

document.addEventListener('DOMContentLoaded', () => {
  const deleteButtons = document.querySelectorAll('.deleteCowBtn');
  const confirmDeleteBtn = document.getElementById('confirmDeleteBtn');
  
  const deleteSheepButtons = document.querySelectorAll('.deleteSheepBtn');
  const confirmDeleteSheepBtn = document.getElementById('confirmDeleteSheepBtn');
  deleteButtons.forEach(btn => {
    btn.addEventListener('click', (e) => {
      e.preventDefault(); // stop default link
      const deleteUrl = btn.getAttribute('data-id');
      confirmDeleteBtn.setAttribute('href', deleteUrl); // set modal button link
      const deleteModal = new bootstrap.Modal(document.getElementById('deleteCowModal'));
      deleteModal.show();
      
    });
  });

  deleteSheepButtons.forEach(btn => {
    btn.addEventListener('click', (e) => {
      e.preventDefault(); // stop default link
      const deleteSheepUrl = btn.getAttribute('data-id');
      confirmDeleteSheepBtn.setAttribute('href', deleteSheepUrl); // set modal button link
      const deleteSheepModal = new bootstrap.Modal(document.getElementById('deleteSheepModal'));
      deleteSheepModal.show();
      
    });
  });
});
</script>
<div id="topnotch" class="topnotch">
  <svg
    xmlns="http://www.w3.org/2000/svg"
    viewBox="0 0 100 20"
    width="200"
    height="40"
  >
    <!-- Notch shape -->
    <path
      d="M 0 0
         L 100 0
         C 95 0, 95 20, 85 20
         L 15 20
         C 5 20, 5 0, 0 0
         Z"
      fill="#2a2f33"
      stroke="#2a2f33"
    />

    <!-- Text centered -->
    <text
      id="breedLabel"
      x="50%"
      y="55%"
      dominant-baseline="middle"
      text-anchor="middle"
      fill="white"
      font-size="8"
      font-family="sans-serif"
      font-weight="bold"
    ></text>
  </svg>
</div>
<div class="container" style="margin-top: 50px;">
  <div class="row my-4">
    <div class="col-lg-12">
      <% if (message) { %>
        <div class="alert alert-dismissible fade show alert-<%= message.type %>" role="alert">
          <button class="btn-close" type="button" data-bs-dismiss="alert" aria-label="close"></button>
          <strong><%= message.message %></strong>
        </div>
      <% } %>
        <div id="sheepTableContainer" class="hide">
          <%- include('Animal_Tables/table_sheep') %>
        </div>

        <div id="cowTableContainerAngus" class="hide">
          <%- include('Animal_Tables/table_angus') %>
        </div>

        <div id="cowTableContainerLimousine" class="hide">
          <%- include('Animal_Tables/table_limousine') %>
        </div>

        
    </div>
  </div>
</div>

<%- include('../components/cowsInfo') %>
<%- include('../components/sheepInfo') %>
<%- include('../components/deleteSheepModal') %>
<%- include('../components/edit_cows') %>
<%- include('../components/deleteCowModal') %>
<%- include('../components/edit_sheep') %>

<script src="/FillEditModalInfo.js"></script>
<script src="/FillEditModalInfoSheep.js"></script>
<script src="/FillEditModal.js"></script>
<script src="/FillEditModalSheep.js"></script>

<%- include('layout/footer')  %>




